<div class="form">
	<%= form_for [:admin, @post] do |f| %>
		<%= f.label :title %>
		<%= f.text_field :title %>
		<%= f.label :content %>
		<%= f.text_field :content %>
		<%= submit_tag 'Submit blog post' %>
	<%end%>
</div>