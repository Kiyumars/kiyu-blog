
	<%= form_for [:admin, @post] do |f| %>
		<%= f.label :title %>
		<%= f.text_field :title, value: @post.title %>
		<%= f.label :content %>
		<%= f.text_area :content, value: @post.content %>
		<%= submit_tag 'Submit blog post' %>
	<%end%>
